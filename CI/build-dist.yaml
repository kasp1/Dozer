
steps:
#
# Build
#
# - displayName: Semantic Release
#   command: npx semantic-release --no-ci --debug

- displayName: Build Runner
  command: echo Building runner

- displayName: Build WebUI
  command: cd "webui" && npm run build & cd ..
